# USERS CRUD API

# ROUTES
    /users  => GET && POST
    /users/:id => GET 

# DATA FOR CREATE USERS AND UPDATE

 {
    "id": 1,
    "first_name": "string",
    "last_name": "string",
    "email": "string",
    "password": "string",
    "birthday": "YYYY/MM/DD"
 }
